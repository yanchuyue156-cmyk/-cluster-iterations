# 预测码头与真实码头对比：iteration_04_terminal_max_span_3000m

## 匹配方法

1. 预测码头使用 `china_terminals.csv` 的 AIS 活动中心（`lat`、`lon`）与预测范围面。
2. 真实码头使用参考数据的 `point` 作为中心、`geom` 作为范围面。
3. 真实码头中心可位于近岸水域，因此评估范围为中国与台湾陆地及其 50 km 近岸海域；落在范围外的参考记录不删除，只是不参与本次中国范围评估。
4. 只保留两中心距离不超过 2,000 m 的候选对，并采用最小总中心距离的一对一匹配。每个预测码头和每个真实码头最多匹配一次；不能在半径内匹配的对象视为未匹配。
5. 所有面积与叠加指标均在 EPSG:3857 米制坐标中计算。

## 覆盖与匹配

| 项目 | 数量 |
| --- | ---: |
| 预测码头 | 738 |
| 参考码头原始记录 | 931 |
| 因中心在中国及 50 km 近岸海域外而不参与评估的参考记录 | 9 |
| 参与评估的参考码头 | 922 |
| 一对一匹配对 | 482 |
| 未匹配预测码头 | 256 |
| 未匹配参考码头 | 440 |

## 中心位置

| 指标 | 数值 |
| --- | ---: |
| 中心距离中位数 | 393 m |
| 中心距离 P90 | 1141 m |

## 面积

面积比定义为 `预测面积 / 真实面积`。

| 指标 | 数量/数值 |
| --- | ---: |
| 预测面积小于真实面积 | 205 (42.5%) |
| 预测面积大于真实面积 | 277 (57.5%) |
| 面积比中位数 | 1.334 |
| 面积比 P25 / P75 | 0.509 / 2.648 |
| 面积在真实面积 0.5--2 倍内 | 189 (39.2%) |

## 形状重叠

- IoU = `预测面与真实面交集 / 两者并集`，越接近 1 表示形状与位置越一致。
- 预测面积精度 = `交集 / 预测面积`，用于判断预测范围有多少落在真实范围内。
- 真实面积召回 = `交集 / 真实面积`，用于判断真实范围被预测覆盖了多少。

| 指标 | 中位数 | P90 |
| --- | ---: | ---: |
| IoU | 0.301 | 0.526 |
| 预测面积精度 | 0.510 | 0.959 |
| 真实面积召回 | 0.645 | 1.000 |

逐对结果见 `terminal_comparison_pairs.csv`。该报告评价的是参考数据中记录的码头范围与 AIS 推断活动范围的一致性，不应被解释为法定或运营边界的真值判定。
