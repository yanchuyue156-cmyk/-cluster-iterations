# AIS 停泊点识别参数说明

本文记录正式流程 `python -m src.common.main` 使用的参数。参数定义位于 `src/common/pipeline.py` 的 `PipelineConfig`；除非修改代码，正式运行均使用下列默认值。

## 处理流程

```text
AIS 轨迹 → 停泊段 → 泊位型 / 锚地型停泊 → 泊位、锚地聚类 → 由泊位聚成码头
```

“码头”指空间相邻泊位组成的预测码头范围，不代表行政意义上的港口。

## 停泊段识别

| 参数 | 默认值 | 含义 |
| --- | ---: | --- |
| `stop_speed_kn` | 1.0 节 | 同一艘船相邻 AIS 点的推算速度低于此值时，记录为停泊状态。 |
| `min_stop_minutes` | 60 分钟 | 连续停泊状态至少持续此时长，才保留为有效停泊段。 |

## 泊位识别与聚类

| 参数 | 默认值 | 含义 |
| --- | ---: | --- |
| `berth_drift_max_m` | 150 米 | 停泊段内的最大漂移小于此值时，判为泊位型停泊。 |
| `berth_eps_m` | 150 米 | 泊位型停泊段的 DBSCAN 聚类半径。 |
| `berth_min_samples` | 2 | 一个泊位聚类至少需要的停泊段数量。 |
| `berth_outline_buffer_m` | 0 米 | 在泊位聚类和 AIS 中心确定后，对泊位活动范围面额外向外扩展的距离；不参与泊位聚类。 |
| `berth_coast_max_m` | 4,000 米 | 距海岸超过此距离的泊位候选会过滤；但落在中国陆地区域内的候选保留。 |

## 码头聚类与范围生成

| 参数 | 默认值 | 含义 |
| --- | ---: | --- |
| `terminal_eps_m` | 1,000 米 | 预测泊位的 AIS 活动中心的 DBSCAN 聚类半径；相邻泊位据此归入同一码头。 |
| `terminal_min_samples` | 1 | 一个泊位也可单独形成预测码头。 |
| `terminal_max_span_m` | 不限制 | 单一码头在范围外扩前允许的最大直线跨度。启用后只切分既有 DBSCAN 类别，避免相邻泊位链被串成超长码头。 |
| `terminal_merge_buffer_m` | 180 米 | 合并码头内泊位范围时使用的缓冲距离。 |
| `terminal_outline_buffer_m` | 0 米 | 在码头归属和 AIS 中心确定后，对活动范围面额外向外扩展的距离；不参与聚类。 |
| `terminal_shape_method` | `merged_outline` | 码头范围面方法：`merged_outline` 保留合并后的活动轮廓；`bounding_rectangle` 将最终范围面改为严格矩形，不影响聚类或中心。 |
| `terminal_resolve_rectangle_overlaps` | `False` | 仅在 `bounding_rectangle` 下生效；沿相邻码头 AIS 中心的主方向切开重叠部分，保持矩形且不改变码头中心或聚类。 |
| `terminal_simplify_m` | 35 米 | 码头轮廓的边界简化尺度。 |

泊位中心取停泊时长加权的 AIS 停泊 medoid；码头中心再取所属泊位中心的时长加权 medoid。中心始终是一个真实 AIS 停泊位置，而不是活动范围面或多边形的几何质心。每次正式运行会将码头结果写入 `china_terminals.csv`，主键为 `terminal_id`。

## 锚地识别、聚类与范围生成

| 参数 | 默认值 | 含义 |
| --- | ---: | --- |
| `anchorage_drift_min_m` | 300 米 | 停泊段漂移达到此值后，才可能判为锚地型停泊。 |
| `anchorage_duration_min` | 1,440 分钟（24 小时） | 锚地型停泊至少需要的持续时长。 |
| `anchorage_eps_m` | 6,000 米 | 锚地型停泊段的 DBSCAN 聚类半径。 |
| `anchorage_min_samples` | 5 | 一个锚地聚类至少需要的停泊段数量。 |
| `anchorage_max_span_m` | 不限制 | 启用后，在初始 DBSCAN 锚地簇内以 complete linkage 切分，限制每个子簇的最大空间跨度，防止链式连通。 |
| `anchorage_shape_method` | `concave_hull` | 锚地范围生成方法：`concave_hull` 使用凹包；`point_buffer_union` 使用停泊点生成的圆角外边界；`center_buffer` 以时长加权 AIS 中心生成自适应圆形范围。 |
| `anchorage_center_coverage_quantile` | 0.50 | 使用 `center_buffer` 时，半径覆盖从中心向外累计到此比例的停泊时长；例如 0.90 表示覆盖 90%。 |
| `anchorage_concavity_ratio` | 0.10 | 生成锚地凹包时的凹度比例；数值越小，轮廓越贴近点群。 |
| `anchorage_outline_buffer_m` | 180 米 | 锚地范围的缓冲距离；使用 `point_buffer_union` 时是每个停泊点的局部缓冲半径。 |
| `anchorage_adaptive_buffer_min_m` / `anchorage_adaptive_buffer_max_m` | 0 米 / 0 米 | 使用 `adaptive_point_buffer_union` 时的外扩距离下限与上限。 |
| `anchorage_adaptive_buffer_span_ratio` | 0 | 使用 `adaptive_point_buffer_union` 时，点群包络对角线乘以此比例得到外扩距离，再由上下限裁剪。 |
| `anchorage_simplify_m` | 40 米 | 锚地轮廓的边界简化尺度。 |
| `anchorage_exclude_terminal_overlap` | `False` | 启用后，使用固定码头范围作为锚地排除区：中心落在码头内的锚地剔除，其余重叠范围从锚地面中裁掉。 |
| `anchorage_terminal_clearance_m` | 0 米 | 锚地排除区在固定码头范围外额外保留的安全间隔，用于消除坐标转换造成的边界细缝。 |
| `anchorage_exclude_land_overlap` | `False` | 启用后，锚地中心不能落在中国陆地区域内，锚地范围面也会裁掉陆地与河岸内侧部分。 |
| `anchorage_land_clearance_m` | 0 米 | 锚地陆地排除区在陆地边界外额外保留的安全间隔。 |
| `anchorage_use_land_boundary_distance` | `False` | 启用后，锚地的距岸过滤改为计算到中国陆地边界的距离，包含河岸和岛岸。 |
| `anchorage_open_water_only` | `False` | 启用后，锚地范围中距陆地边界不足现有 `anchorage_coast_min_m` 的部分会裁掉，以排除狭窄内河和贴岸水道；此模式不因中心贴岸而删除整块向海范围。 |
| `anchorage_coast_min_m` | 3,000 米 | 非开阔水域模式下，距海岸不足此距离的锚地中心会过滤；开阔水域模式下，作为锚地范围面距陆地边界的最小净距。 |
| `anchorage_coast_max_m` | 20,000 米 | 距海岸超过此距离的锚地候选会过滤。 |

## 数据范围与运行参数

| 参数 | 默认值 | 含义 |
| --- | ---: | --- |
| `china_lon_min` / `china_lon_max` | 73 / 135 | 保留 AIS 数据的经度范围。 |
| `china_lat_min` / `china_lat_max` | 18 / 54 | 保留 AIS 数据的纬度范围。 |
| `workers` | 8 | 并发读取 AIS CSV 的线程数；可用 `--workers` 临时指定。 |

## 运行方式

```bash
# 使用停泊段缓存，生成下一个结果版本
python -m src.common.main

# AIS 输入数据更新后，重新生成停泊段缓存
python -m src.common.main --refresh-cache

# 调整读取并发数
python -m src.common.main --workers 4
```

> 参数修改会改变预测结果。修改后请运行正式流程生成新的编号结果目录，不要覆盖既有版本。

如需比较不同参数，请在 `src/iterations/` 新建一轮参数方案，而不是复制或直接修改公共 `pipeline.py`；具体方式见 `src/iterations/README.md`。
